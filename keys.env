# ── WRAITH keys — ALL OPTIONAL, bot works without any ──
# AI chatbot last-resort fallback
OPENAI_API_KEY=
OPENROUTER_API_KEY=
# rmbg (faster cloud option; local removal works without it)
BRIA_API_KEY=
HF_TOKEN=
# Instagram fallback (only if keyless endpoints blocked for your IP)
IG_SESSIONID=
